import React from 'react';
import ReactDOM from 'react-dom';

const App = () => (
    <div>
        <h1>Google Ads Integration</h1>
        <p>This project demonstrates Google Ads integration.</p>
    </div>
);

ReactDOM.render(<App />, document.getElementById('root'));